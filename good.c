#include<stdio.h> // define the header file 
#include <math.h>

// Create a function

//When a parameter is passed to the function, it is called an argument. So, from the example above: name is a parameter, while Liam, Jenny and Anja are arguments.

int main(){
    printf("%f\n", ceil(1.3));//function rounds a number upwards to its nearest integer
    printf("%f\n", floor(1.5));//floor() method rounds a number downwards to its nearest integer
    printf("%f", sqrt(16));// find the square root of a number, use the sqrt() function:
    printf("%f", pow(4, 3));//The pow() function returns the value of x to the power of y (xy):

    printf("hello there");
    return 0;
}